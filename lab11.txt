11.1

a. Compare and contrast your development and production deployments and explain how each is designed.

I don't see any difference and basically the lab had me copy and paste random crap that doesn't mean anything to me so there is nothing to compare or contrast and I wasn't really given the correct information to explain the design other than to say if it's a production deployment it probably shouldn't be local anymore except I still have to search "localhost:3000".

b. List the optimizations you’ve configured for this production version and, if possible, find evidence of then in the deployed application files.

I don't see any optimizations of any kind.

11.2
    
a. What advantage is there is deploying an application to a PaaS tool like Heroku?

The application is no longer running locally and will be automatically updated. The workload is not being handled by my laptop anymore.

b. How does Heroku know how to install and run your application (after pulling it from the specified branch on GitHub)?

Heroku uses the package.json file's scripts to build and run the program.
